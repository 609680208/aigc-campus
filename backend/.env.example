DATABASE_URL=postgresql://postgres:postgres@localhost:5432/aigc_campus
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=2h
PORT=3000

# OPC SSO 对接：OPC 侧调用 /api/auth/sso/register 时需携带的共享密钥（留空则不校验）
SSO_SECRET=

# AI 默认中转站（模型未单独配置 baseUrl/apiKey 时回退）
AI_BASE_URL=
AI_API_KEY=